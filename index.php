<?php
// ==============================================================================
// MINE SERVER - Точка входа
// ==============================================================================

session_start();

// Проверка авторизации
if (isset($_SESSION["authenticated"]) && $_SESSION["authenticated"] === true) {
    // Авторизован - показываем панель
    include "cabinet.php";
} else {
    // Не авторизован - показываем логин
    include "login.php";
}
